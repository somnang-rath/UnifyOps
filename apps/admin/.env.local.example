# API origin the next.config rewrite proxies /api/v1/* to (keeps the refresh
# cookie same-origin). Defaults to the local API dev server.
API_URL=http://localhost:4000/api/v1
# Main web app origin — used by the "Back to UnifyOps" link in the admin sidebar.
NEXT_PUBLIC_WEB_URL=http://localhost:3000
