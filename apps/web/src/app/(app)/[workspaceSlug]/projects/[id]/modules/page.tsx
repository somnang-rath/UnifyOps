import { ComingSoon } from '../_components/coming-soon';

export default function ProjectModulesPage() {
  return (
    <ComingSoon
      title="Modules"
      desc="Feature- or goal-based groupings of work items, each with its own lead, members and progress."
    />
  );
}
