import { ComingSoon } from '../_components/coming-soon';

export default function ProjectViewsPage() {
  return (
    <ComingSoon
      title="Views"
      desc="Saved combinations of filters, grouping and layout so your team can jump straight to a slice of work."
    />
  );
}
