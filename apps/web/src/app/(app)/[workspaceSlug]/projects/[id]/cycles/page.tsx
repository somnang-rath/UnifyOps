import { ComingSoon } from '../_components/coming-soon';

export default function ProjectCyclesPage() {
  return (
    <ComingSoon
      title="Cycles"
      desc="Time-boxed sprints to focus a set of work items over a start and end date, with burn-down progress."
    />
  );
}
