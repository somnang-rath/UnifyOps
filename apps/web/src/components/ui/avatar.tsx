/**
 * Moved to @prism/ui (docs/plan/02-design-system.md).
 *
 * Same sm/md/lg pixel sizes as before, so no screen shifted. The package
 * version adds a broken-image fallback to initials and an opt-in `labelled`
 * prop for avatars that stand alone.
 */
export { Avatar, AvatarGroup } from '@prism/ui';
export type { AvatarProps, AvatarSize } from '@prism/ui';
